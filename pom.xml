<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.cismet.cids</groupId>
        <artifactId>cids-parent</artifactId>
        <version>1.0.5</version>
    </parent>

    <groupId>de.cismet.custom</groupId>
    <artifactId>beansbinding</artifactId>
    <version>1.2.2-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>beansbinding</name>
    <description>!!! description pending !!!</description>

    <scm>
        <connection>scm:svn:svn+ssh://repositories/svn/repositories/central/de/cismet/custom/beansbinding/trunk</connection>
        <developerConnection>scm:svn:svn+ssh://repositories/svn/repositories/central/de/cismet/custom/beansbinding/trunk</developerConnection>
        <url>http://repositories:8082/central/browse/de/cismet/custom/beansbinding/trunk</url>
    </scm>

    <properties>
        <!-- 
            ToDo make constants for the single values to ease the configuration
            of the child POMs. 
        -->
        <de.cismet.libDir>lib/ext</de.cismet.libDir>
    </properties>
    
    <dependencies>
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.8</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                overwritten because the artifact has to be renamed due to
                compatibility reasons to the cidsDistribution
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-installed</id>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <destFileName>beansbinding-1.2.1.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>skip-jalopy</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>triemax</groupId>
                        <artifactId>jalopy-maven</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
